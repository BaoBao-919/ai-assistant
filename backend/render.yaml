services:
  - type: web
    name: ai-assistant-backend
    runtime: node
    repo: https://github.com/BaoBao-919/ai-assistant
    branch: main
    rootDir: backend
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false